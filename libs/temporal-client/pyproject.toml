[project]
name = "temporal-client"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "temporalio>=1.9.0",
]

[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
