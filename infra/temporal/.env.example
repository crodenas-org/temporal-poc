# Temporal connection — set these in each service's .env or ECS task definition
TEMPORAL_HOST=localhost:7233
TEMPORAL_NAMESPACE=default

# Override for non-default task queue name (optional — services set their own)
# TEMPORAL_TASK_QUEUE=my-service-queue
